let express,mongoose,cors,socket,fs,path,nodemailer,PORT,MONGODB_URL,SERVER_Path,errorHandler,router,getCurrentUser,userDisconnect,joinUser,getCurrentRoom,checkRoom,totalOnlineUsers,Message,emailService;_b93‍.w("express",[["default",["express"],function(v){express=v}]]);_b93‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_b93‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_b93‍.w("socket.io",[["default",["socket"],function(v){socket=v}]]);_b93‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_b93‍.w("path",[["default",["path"],function(v){path=v}]]);_b93‍.w("nodemailer",[["default",["nodemailer"],function(v){nodemailer=v}]]);_b93‍.w("./config/index.js",[["PORT",["PORT"],function(v){PORT=v}],["MONGODB_URL",["MONGODB_URL"],function(v){MONGODB_URL=v}],["SERVER_Path",["SERVER_Path"],function(v){SERVER_Path=v}]]);_b93‍.w("./errorHandler",[["errorHandler",["errorHandler"],function(v){errorHandler=v}]]);_b93‍.w("./routers",[["default",["router"],function(v){router=v}]]);_b93‍.w("./socketUsers",[["getCurrentUser",["getCurrentUser"],function(v){getCurrentUser=v}],["userDisconnect",["userDisconnect"],function(v){userDisconnect=v}],["joinUser",["joinUser"],function(v){joinUser=v}],["getCurrentRoom",["getCurrentRoom"],function(v){getCurrentRoom=v}],["checkRoom",["checkRoom"],function(v){checkRoom=v}],["totalOnlineUsers",["totalOnlineUsers"],function(v){totalOnlineUsers=v}]]);_b93‍.w("./models/message",[["default",["Message"],function(v){Message=v}]]);_b93‍.w("./services/emailService",[["default",["emailService"],function(v){emailService=v}]]);






// import {fileTypeFromBuffer} from 'file-type';
// import {readChunk} from 'read-chunk';















// import socketIo from "./socketIo";

const app = express();

mongoose.connect(MONGODB_URL, {
  useCreateIndex: true,
  useNewUrlParser: true,
  useUnifiedTopology: true,
  useFindAndModify: false,
});

const db = mongoose.connection;
db.on("error", _b93‍.g.console.error.bind(_b93‍.g.console, "connection error:"));
db.once("open", () => {
  console.log("DB connected...");
});

app.use(cors());
app.use(express.urlencoded({ extended: false }));
app.use(express.json({ limit: "100mb" }));

app.use("/uploads", express.static("uploads"));
app.use("/api", router);

app.use(errorHandler);

//mainUi
app.use(express.static(path.join(__dirname, "react/main/build")));
app.get("/*", (req, res) => {
  res.sendFile(path.resolve(__dirname, "react/main/build", "index.html"));
});

const server = app.listen(PORT, () => {
  console.log(`server runing... on port ${PORT}`);
});
// console.log(server)

global.io = socket(server, { maxHttpBufferSize: 1e9 });
global.appRoot = path.resolve(__dirname);

//initializing the socket io connection
io.on("connection", (socket) => {
  console.log("Socket is Connected");
  //for a new user joining the room
  socket.on("joinChat", ({ sender, _id }) => {
    // username = user.name
    //* create user
    const pUser = joinUser(socket.id, sender, _id);
    _b93‍.g.console.log(socket.id, "=id");
    const isInRoom = socket.rooms.has(_id);

    _b93‍.g.console.log(isInRoom,`this is rooms: ${_id}`);
    socket.join(_id);

    socket.broadcast.emit("userJoin", {
      _id: pUser.sender._id,
      name: pUser.sender.name,
      text: `${pUser.sender.name} has joined the chat`,
    });
    // }
  });

  socket.on("checkOnline", () => {
    const userData = totalOnlineUsers();
    const users = userData.map((e) => e.sender._id);
    socket.emit("onlines", {
      users,
    });
  });

  //user sending message
  socket.on("chat", async (value) => {
    //gets the room user and the message sent
    // const pUser = getCurrentUser(socket.id);

    if (value._id != undefined) {
      // let message = await Message.Schema.statics.create(msg);
      let buffer;
      let filePath;
      let fileurl;
      let fileExt;
      let tempPath;

      if (value.file) {
        const matches = value.file.match(
          /^data:([A-Za-z-+\/\.]+);base64,(.+)$/
        );
        if (!matches) {
          return socket.emit("fileError", { message: "File Size Empty" });
        }
        if (!matches[0]) {
          return socket.emit("fileError", { message: "File Invalid" });
        }
        _b93‍.g.console.log(matches[1]);
        if (matches[1].includes("image")) {
          fileExt = matches[1].split("/")[1];
        } else if (matches[1].includes("pdf")) {
          fileExt = matches[1].split("/")[1];
        } else if (matches[1].includes("application/vnd.ms-excel")) {
          fileExt = "xls";
        } else if (matches[1].includes("ms-excel")) {
          fileExt = "csv";
        } else if (
          matches[1].includes(
            "vnd.openxmlformats-officedocument.wordprocessingml"
          )
        ) {
          fileExt = "docx";
        } else if (
          matches[1].includes(
            "vnd.openxmlformats-officedocument.presentationml"
          )
        ) {
          fileExt = "pptx";
        } else if (matches[1].includes("text/plain")) {
          fileExt = "txt";
        } else if (matches[1].includes("x-zip-compressed")) {
          fileExt = "zip";
        } else if (matches[1].includes("application/zip")) {
          fileExt = "zip";
        } else if (matches[1].includes("application/octet-stream")) {
          fileExt = "rar";
        } else if (
          matches[1].includes(
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
          )
        ) {
          fileExt = "xlsx";
        }
        if (!fileExt) {
          return socket.emit("fileError", { message: "File type invalid" });
        }

        buffer = Buffer.from(matches[2], "base64");
        filePath = `${Date.now()}-${Math.round(
          Math.random() * 1e9
        )}.${fileExt}`;
        fileurl = `/uploads/${filePath}`;
        tempPath = `${SERVER_Path}${fileurl}`;

        try {
          fs.writeFile(`./uploads/${filePath}`, buffer, "binary", (err) => {
            if (err) {
              _b93‍.g.console.log(err);
            }
          });
        } catch (err) {
          _b93‍.g.console.log(err);
        }
      }

      _b93‍.g.console.log(value.text, 'socketMessge')
      if (value.orderId) {
        socket.emit("orderMessage", {
          _id: value._id,
          name: value.sender.name,
          text: value.text,
          sender: value.sender,
          orderId: value.orderId,
          order: value.order ? value.order : {},
          ...(value.file && { file: [tempPath] }),
        });
        socket.in(value.receiver).emit("orderMessage", {
          _id: value._id,
          name: value.sender.name,
          text: value.text,
          sender: value.sender,
          orderId: value.orderId,
          order: value.order ? value.order : {},
          ...(value.file && { file: [tempPath] }),
        });
      } else {
        io.sockets.in(value._id).emit("message", {
          _id: value._id,
          name: value.sender.name,
          text: value.text,
          sender: value.sender,
          ...(value.file && { file: [tempPath] }),
        });
      }
      try {
        await Message.create(
          value.text,
          value.sender._id,
          value.sender.role,
          fileurl,
          value.receiver,
          value.orderId
        );
      } catch (err) {
        _b93‍.g.console.log(err);
      }
      // const roommate = getCurrentRoom(value._id);
      const clientsInRoom = io.sockets.adapter.rooms.get(value.receiver);
      if(clientsInRoom){
        if(clientsInRoom.size){
          _b93‍.g.console.log(clientsInRoom.size, `${value.receiver} room size is ${clientsInRoom.size}`)
        }
      }else{
        console.log(`${value.receiver} room is empty`)
      }
      _b93‍.g.console.log(clientsInRoom,'ClientInRoom')

      // if (!isInRoom) {
      //   console.log(isInRoom, "client is in the room!");
      //   // emailService.setMailToOfflineUser(value.receiver, value.text);
      // }
    }
  });
  socket.on("forceDisconnect", (id) =>{
    socket.disconnect(true);
    socket.leave(id);
    console.log(`${id} left room`);
  });

  socket.on("disconnect", () => {
    //the user is deleted from array of users and a left room message displayed
    const dUser = userDisconnect(socket.id);
    _b93‍.g.console.log(dUser, "disconnected");
    if (dUser) {
      socket.broadcast.emit("userDisconnet", {
        _id: dUser.sender._id,
        name: dUser.sender.name,
        text: `${dUser.sender._id} has left the chat`,
      });
    }
  });
});
