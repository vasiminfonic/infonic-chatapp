let mongoose,User;_e9f‍.x([["default",()=>_e9f‍.o]]);_e9f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_e9f‍.w("./user",[["default",["User"],function(v){User=v}]]);



const messageSchema = mongoose.Schema({
    message:{type: String, required: true},
    type:{type: String, default:'user'},
    file: [String],
    sender: {type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true},
    receiver: {type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true},
    seen: {type: Boolean, default: false}
}, {timestamps: true})



messageSchema.statics.create = (content, sender, type, file, receiver) => {
    let msg = new messageModal({
        message: content,
        type: type,
        sender: sender,
        receiver: receiver,
        ...(file && {file: file})
    });
    return msg.save();
}
messageSchema.statics.latest = (count) => {
    return this.find({}).sort({"_id": "desc"}).limit(count);
};

const messageModal = mongoose.model("Message", messageSchema, 'messages');

_e9f‍.d(messageModal);