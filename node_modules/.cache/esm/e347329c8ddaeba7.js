let express,mongoose,cors,socket,fs,path,nodemailer,PORT,MONGODB_URL,SERVER_Path,errorHandler,router,getCurrentUser,userDisconnect,joinUser,getCurrentRoom,checkRoom,Message;_8dd‍.w("express",[["default",["express"],function(v){express=v}]]);_8dd‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_8dd‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_8dd‍.w("socket.io",[["default",["socket"],function(v){socket=v}]]);_8dd‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_8dd‍.w("path",[["default",["path"],function(v){path=v}]]);_8dd‍.w("nodemailer",[["default",["nodemailer"],function(v){nodemailer=v}]]);_8dd‍.w("./config",[["PORT",["PORT"],function(v){PORT=v}],["MONGODB_URL",["MONGODB_URL"],function(v){MONGODB_URL=v}],["SERVER_Path",["SERVER_Path"],function(v){SERVER_Path=v}]]);_8dd‍.w("./errorHandler",[["errorHandler",["errorHandler"],function(v){errorHandler=v}]]);_8dd‍.w("./routers",[["default",["router"],function(v){router=v}]]);_8dd‍.w("./socketUsers",[["getCurrentUser",["getCurrentUser"],function(v){getCurrentUser=v}],["userDisconnect",["userDisconnect"],function(v){userDisconnect=v}],["joinUser",["joinUser"],function(v){joinUser=v}],["getCurrentRoom",["getCurrentRoom"],function(v){getCurrentRoom=v}],["checkRoom",["checkRoom"],function(v){checkRoom=v}]]);_8dd‍.w("./models/message",[["default",["Message"],function(v){Message=v}]]);






// import {fileTypeFromBuffer} from 'file-type';
// import {readChunk} from 'read-chunk';









// import socketIo from "./socketIo";


const app = express();

mongoose.connect(MONGODB_URL, {
  useCreateIndex: true,
  useNewUrlParser: true,
  useUnifiedTopology: true,
  useFindAndModify: false,
})

const db = mongoose.connection;
db.on('error', _8dd‍.g.console.error.bind(_8dd‍.g.console, 'connection error:'));
db.once('open', () => {
  console.log('DB connected...');
});

app.use(cors());
app.use(express.urlencoded({ extended: false }));
app.use(express.json());


app.use('/uploads', express.static('uploads'));
app.use('/api', router);

app.use(errorHandler);


//mainUi
app.use(express.static(path.join(__dirname, 'react/main/build')));
app.get('/*', (req, res) => {
  res.sendFile(path.resolve(__dirname, 'react/main/build', 'index.html'));
});


const server = app.listen(PORT, () => {
  console.log(`server runing... on port ${PORT}`);
})
// console.log(server)





global.io = socket(server);

//initializing the socket io connection 
io.on("connection", (socket) => {
  console.log('Socket is Connected')
  //for a new user joining the room
  let id;
  let username;
  socket.on("joinChat", ({sender, _id}) => {
    // username = user.name
    //* create user
    const pUser = joinUser(socket.id, user, _id);
    // console.log(_id, "=id");

    socket.join(pUser.room);

    //display a welcome message to the user who have joined a room
  //   if(pUser.user){
  //   socket.emit("message", {
  //     _id: _id,
  //     name: pUser.user.name,
  //     text: `Welcome ${pUser.user.name}`,
  //     type: pUser.user.role
  //   });
  //   //displays a joined room message to all other room users except that particular user
  //   socket.broadcast.emit("userJoin", {
  //     _id: _id,
  //     name: pUser.user.name,
  //     text: `${pUser.user.name} has joined the chat`,
  //   });
  // }
  });
  //user sending message
  socket.on("chat", async (value) => {
    //gets the room user and the message sent
    // const pUser = getCurrentUser(socket.id);
    _8dd‍.g.console.log(value.text,'socket');
    

    if (value._id != undefined) {
      // let message = await Message.Schema.statics.create(msg);
      let buffer;
      let filePath;
      let fileurl;
      let fileExt;
      if (value.file) {
       
        var matches = value.file.match(/^data:([A-Za-z-+\/\.]+);base64,(.+)$/);     
        _8dd‍.g.console.log(matches[1]);

        if(matches[1].includes('image')){
          fileExt = matches[1].split('/')[1];
        } else if(matches[1].includes('pdf')){
          fileExt = matches[1].split('/')[1];
        }else if(matches[1].includes('ms-excel')){
          fileExt = 'csv';
        }else if(matches[1].includes('vnd.openxmlformats-officedocument.wordprocessingml')){
          fileExt = 'docx';
        }else if(matches[1].includes('vnd.openxmlformats-officedocument.presentationml')){
          fileExt = 'pptx';
        }else if(matches[1].includes('text/plain')){
          fileExt = 'txt';
        }
        else if(matches[1].includes('x-zip-compressed')){
          fileExt = 'zip';
        }

        buffer = Buffer.from(matches[2], 'base64');
        filePath = `${Date.now()}-${Math.round(
          Math.random() * 1e9
        )}.${fileExt}`;
        fileurl = `${SERVER_Path}/uploads/${filePath}`;

        try {
          fs.writeFile(`./uploads/${filePath}`, buffer, 'binary',(err) => {
            if (err) {
              _8dd‍.g.console.log(err);
            }
          })
        } catch (err) {
          _8dd‍.g.console.log(err);
        }
      }

      io.to(value._id).emit("message", {
        _id: value._id,
        name: value.sender.name,
        text: value.text,
        sender: value.sender,
        ...(value.file && { file: [fileurl] })
      });
      socket.broadcast.emit("userMessage", {
        _id: value._id,
        name: value.sender.name,
        message: `${value.text}`
      });
      try {
        await Message.create(value.text, value.sender._id, value.sender.role, fileurl, value.receiver);
      } catch(err) {
        _8dd‍.g.console.log(err);
      }
    // const roommate = getCurrentRoom(value._id);
    const { empty } = checkRoom(value._id);
    _8dd‍.g.console.log(empty, 'checkfdkljasd');

    if(empty ==='admin' || empty === 'user'){
      const emailId = empty === 'admin' ? '' : value._id 
      // const email = await User.email(emailId);
      // console.log(email, 'emait--$$$$');

      let transporter = nodemailer.createTransport({
        host: "smtp.gmail.com",
        port: 465,
        secure: true, // true for 465, false for other ports
        auth: {
          user: 'vasim.infonic@gmail.com', // generated ethereal user
          pass: 'qiwpdofprhvkeevk', // generated ethereal password
        },
      });
      let info = await transporter.sendMail({
        from: 'vasim.infonic@gmail.com', // sender address
        to: 'vasim.infonic@gmail.com', // list of receivers
        subject: "new Message from infonic", // Subject line
        text: value.text, // plain text body
        html: `<b>Hello world<br>${value.text}</b>`, // html body
      });
      _8dd‍.g.console.log(info);
    }
    }

  });
  socket.on("disconnect", () => {
    //the user is deleted from array of users and a left room message displayed
    const p_user = userDisconnect(socket.id);
    if (id != undefined) {
      io.to(id).emit("message", {
        _id: id,
        name: username,
        text: `${username} has left the chat`,
      });
    }
  });
});





