let mongoose,SERVER_Path,AutoIncrement;_dee‍.x([["default",()=>_dee‍.o]]);_dee‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_dee‍.w("../config",[["SERVER_Path",["SERVER_Path"],function(v){SERVER_Path=v}]]);_dee‍.w("./autoIncrement",[["default",["AutoIncrement"],function(v){AutoIncrement=v}]]);



const translationSchema = new mongoose.Schema(
  {
    userId: {
      type: mongoose.Schema.Types.ObjectId,
      ref: "User",
      required: true,
      auto: true,
    },
    translationId: { type: String, required: false },
    websiteId: { type: String, required: false },
    service_req: { type: String, required: true },
    sourceLanguage: { type: String, required: true },
    targetlanguage: { type: String, required: true },
    phone: { type: String, required: false },
    country: { type: String, required: true },
    deadline: { type: Date, required: true },
    your_words: { type: String, required: true },
    certification: { type: String, required: true },
    message: { type: String, required: false },
    notarization: { type: String, required: false },
    status: { type: String, required: false, default: "new" },
    files: [
      {
        type: String,
        get: (image) => (image ? `${SERVER_Path}/${image}` : ""),
      },
    ],
  },
  { timestamps: true, toJSON: { getters: true }, id: false }
);

translationSchema.pre("save", function (next) {
  const data = this;
  AutoIncrement.findOneAndUpdate(
    { name: "translationOrders" },
    { $inc: { counter: 1 } },
    { upsert: true },
    function (e, r) {
      if (e) {
        return next(e);
      }
      const count = r.counter.toString();
      const d = new Date();
      const Id = `STS${d.getFullYear()}${
        d.getMonth() + 1
      }${d.getDate()}${count.padStart(4, 0)}`;

      data.translationId = Id;
      next();
    }
  );
});

translationSchema.methods.genAssignId = function (count) {
  return;
};
// STS20200316184;

var translationModel = mongoose.model(
  "TranslationOrder",
  translationSchema,
  "translationOrders"
);

_dee‍.d(translationModel);
